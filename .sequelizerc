  
const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "config", "configDatabase.js"),
  "models-path": path.resolve('src', 'api', 'models'),
  "migrations-path": path.resolve(__dirname, "config", "database", "migrations"),
};